import { EngeeTransitionDirective } from './engee-transition.directive';

describe('EngeeTransitionDirective', () => {
  it('should create an instance', () => {
    const directive = new EngeeTransitionDirective();
    expect(directive).toBeTruthy();
  });
});
